"""
src/core — shared infrastructure for the entire application.

Exports:
  exceptions  — custom exception hierarchy
  logger      — centralised logging setup helper
  utils       — general-purpose utilities (filename sanitisation, etc.)
"""